open Stdlib

type List a =
     | Nil
     | Cons a (List a)

let rec map x : forall a b, (List a, List b) -> List a =
    match x with
    | ('Nil, 'Nil) => Nil
    end
    
let main x : () -> () =
    ()

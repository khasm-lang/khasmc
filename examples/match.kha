open Stdlib

type List a =
     | Nil
     | Cons a (List a)

let rec map f x : forall q w, (q -> w) -> List q -> List w =
    match x with
    | 'Nil => Nil
    | Cons q w => Cons (f q) (map f w)
    end
    
let main q : () -> () =
    ()

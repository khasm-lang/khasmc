(*
    Compile with:
    dune exec khasmc test/stdlib.kha test/fib.kha
*)

(* this is a hack to get recursion working *)
let fib n : int -> int = 6

let fib n : int -> int =
    if n = 0 then n
    else if n = 1 then n
    else (fib (n - 1) + fib (n - 2))

let main () : () -> () = show (fib 10)
